# Tailspend

## Repository location

- **Path:** `/Users/countryinterests/Documents/Github/Tailspend`
- **Parent folder (shared tools / venv):** `/Users/countryinterests/Documents/Github`

## Notes (quick reference)

- **Virtual environment** lives next to this repo, not inside it: `…/Github/venv`
- After activation, `cd` into this repo when working on Tailspend.
- Cursor project rules live under `.cursor/rules/` so humans and the agent share the same setup notes.

## Activate the virtual environment

From a terminal:

```bash
cd /Users/countryinterests/Documents/Github
source venv/bin/activate
```

Then open the project:

```bash
cd /Users/countryinterests/Documents/Github/Tailspend
```

**Deactivate** when finished:

```bash
deactivate
```

## Verify

```bash
which python
python --version
```

You should see the interpreter inside `…/Github/venv/`.
